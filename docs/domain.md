# 可疑转账止付协同中枢领域约定

    状态机必须能从事件序列复算，查询时按机构边界裁剪证据。

    ## 核心对象

    - 转账处置：止付期限与当前状态
- 机构事件：签名、幂等键和业务序号
- 核验挑战：短期凭据及完成结果
- 待投递消息：与状态提交同事务写入

    ## 业务边界

    - 相互冲突的并发决定只能有一个生效
- 重启后继续投递但不重复确认消息
- 无关机构不能读取完整证据

    所有时间字段使用带时区的 ISO 8601 表示；公开标识与数据库自增键分离。接口返回的原因码必须稳定，便于调用方记录和重试。
